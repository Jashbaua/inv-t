import { Routes } from '@angular/router';
import { InvManagerComponent } from './inv-manager/inv-manager.component';

export const routes: Routes = [
    {
        path:"",
        component:InvManagerComponent
    }
];
